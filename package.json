{
  "name": "pika-web-crawler-test",
  "version": "0.2.1",
  "description": "Web crawler test script",
  "bin": {
    "pika-web-crawler-test": "./bin/run"
  },
  "keywords": [
    "pika",
    "web",
    "crawler",
    "test"
  ],
  "scripts": {
    "run": "bin/run",
    "posttest": "tsc -p test --noEmit",
    "prepack": "rm -rf lib && tsc",
    "prepare": "rm -rf lib && tsc",
    "test": "nyc mocha --forbid-only \"test/**/*.test.ts\"",
    "prettier": "prettier --parser typescript --single-quote --print-width 120 --write \"./**/*.ts\""
  },
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/pikamachu/pika-web-crawler-test"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "author": "Antonio Marin",
  "license": "MIT",
  "dependencies": {
    "@oclif/command": "^1",
    "@oclif/config": "^1",
    "@oclif/plugin-help": "^2",
    "@types/cheerio": "^0.22.8",
    "cheerio": "^1.0.0-rc.2",
    "chrome-launcher": "^0.10.2",
    "chromeless": "^1.5.0",
    "tslib": "^1"
  },
  "devDependencies": {
    "@oclif/test": "^1",
    "@types/chai": "^4",
    "@types/mocha": "^5",
    "@types/node": "^10",
    "chai": "^4",
    "mocha": "^5",
    "nyc": "^12",
    "ts-node": "^6",
    "typescript": "^2.9",
    "prettier": "^1.12.0"
  },
  "files": [
    "/bin",
    "/lib"
  ],
  "oclif": {
    "bin": "pika-web-crawler-test"
  },
  "types": "lib/index.d.ts"
}
